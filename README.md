# VCF_to_CSV
This script converts one or more vCard (.vcf) contact files into a single Excel-safe CSV file, preserving key contact information and correctly handling multiple phone numbers per contact. It is designed to be reliable on macOS by using AppleScript for user interaction and embedded Python for robust vCard parsing and CSV generation.
